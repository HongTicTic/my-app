## Bug 1: crash (`.map()` on null state)
- **Symptom:**
- **Tool:** (breakpoint / Sources panel / call stack)
- **What it showed:**
- **Fix:**

## Bug 2: silent wrong value (prop/field name typo)
- **Symptom:**
- **Tool:** (React DevTools, Components tab)
- **What it showed:**
- **Fix:**

## Bug 3: network failure (mistyped URL)
- **Symptom:**
- **Tool:** (Network tab)
- **What it showed:**
- **Fix:**

## A breakpoint caught the null .map() crash, React DevTools caught the inStok typo, and the Network tab caught the bad URL, and the console alone wasn't enough because it printed only an error message

